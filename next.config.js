/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: false,
    // Add any remote domains if needed
    // domains: ['your-domain.com'],
  },
}

module.exports = nextConfig 